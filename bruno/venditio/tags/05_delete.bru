meta {
  name: Delete tags
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/tags/:id-or-slug
  body: none
  auth: inherit
}

params:query {
  ~force: 0
  ~delete_children: 0
}

params:path {
  id-or-slug: 
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

docs {
  summary:
  - Delete tags

  endpoint:
  - method: DELETE
  - path: /tags/:id-or-slug

  query params:
  - force (boolean): Optional. When true, deletes even if products are connected and clears related taggable rows.
  - delete_children (boolean): Optional. When true, recursively deletes every child tag below the target.
  - Without force, deleting a tag with connected products returns a validation error on `products`.
  - By default, deleting a tag clears taggable associations, moves direct child tags to the deleted tag's parent, and rebuilds their tree paths.

  path params:
  - id-or-slug: Replace with target resource identifier.

  body:
  - none
}
