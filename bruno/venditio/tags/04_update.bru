meta {
  name: Update tags
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/tags/:id-or-slug
  body: json
  auth: inherit
}

params:query {
  ~include: product_type,products,brands,product_categories,product_collections,tags,discounts,valid_discounts,expired_discounts
}

params:path {
  id-or-slug: 
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

body:json {
  {
    "name": "Updated tag",
    "product_type_id": 1,
    "metadata": {
      "titolo": "Updated tag",
      "autore": null,
      "descrizione": null,
      "robots": null,
      "open_graph_titolo": "Updated tag",
      "open_graph_url": null,
      "open_graph_immagine": null,
      "open_graph_tipo": null,
      "twitter_card": null,
      "twitter_site": null,
      "twitter_creator": null,
      "twitter_titolo": "Updated tag",
      "twitter_descrizione": null,
      "twitter_src": null
    },
    "product_ids": [1],
    "brand_ids": [1],
    "product_category_ids": [1],
    "product_collection_ids": [1],
    "tag_ids": [2, 3]
  }
}

docs {
  summary:
  - Update tags

  endpoint:
  - method: PATCH
  - path: /tags/:id-or-slug

  query params:
  - include (string|array): Optional includes: product_type, products, brands, product_categories, product_collections, tags, discounts, valid_discounts, expired_discounts.

  path params:
  - id-or-slug: Replace with target resource identifier.

  body:
  - JSON payload.
  - Available fields:
  - parent_id
  - product_type_id
  - name
  - slug
  - abstract
  - description
  - metadata (array)
  - images (array|nullable)
  - images.*.id
  - images.*.file (file|image)
  - images.*.alt
  - images.*.name
  - images.*.mimetype
  - images.*.sort_order
  - images.*.type (nullable, thumb|cover)
  - active
  - show_in_menu
  - highlighted
  - sort_order (integer, starts at 1 within each tree branch)
  - visible_from
  - visible_until
  - product_ids (array): polymorphic link to products.
  - product_ids.*
  - brand_ids (array): polymorphic link to brands.
  - brand_ids.*
  - product_category_ids (array): polymorphic link to product categories.
  - product_category_ids.*
  - product_collection_ids (array): polymorphic link to product collections.
  - product_collection_ids.*
  - tag_ids (array): polymorphic link to child tags.
  - tag_ids.*
  - {locale}.name / name:{locale}
  - {locale}.slug / slug:{locale}
  - {locale}.abstract / abstract:{locale}
  - {locale}.description / description:{locale}
  - Note: for image upload use multipart/form-data with fields like `images[0][file]` and `images[0][type]=thumb`.
  - Note: uploaded catalog images are stored under date subfolders (`YYYY/MM/DD`) inside their image type folder.
  - Note: existing images may be updated by id; only one `thumb` and one `cover` are allowed. A request can replace the current `thumb` or `cover` when it also releases the old typed image in the same `images` payload. `type=null` can be repeated for generic images.
}
