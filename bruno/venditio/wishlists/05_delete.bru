meta {
  name: Delete wishlists
  type: http
  seq: 95
}

delete {
  url: {{baseUrl}}/wishlists/:id-or-slug
  body: none
  auth: inherit
}

params:path {
  id-or-slug: 
}

headers {
  Accept: application/json
  Content-Type: application/json
}

docs {
  summary:
  - Delete wishlists

  endpoint:
  - method: DELETE
  - path: /wishlists/:id-or-slug

  query params:
  - none

  path params:
  - id-or-slug: Use a canonical positive integer wishlist ID, or any wishlist slug. Leading-zero or mixed values (for example `001` or `01-slug`) resolve as slugs.

  body:
  - none
}
