meta {
  name: Store products
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/products
  body: json
  auth: inherit
}

params:query {
  ~include: variants,variants_options_table,brand,categories,collections,related_products,discounts,valid_discounts,expired_discounts,tags,product_type,tax_class,price_lists,price_breakdown
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
  Country-ISO-2: {{countryIso2}}
}

body:json {
  {
    "name": "T-shirt",
    "status": "published",
    "product_type_id": 1,
    "category_ids": [1],
    "collection_ids": [1],
    "related_product_ids": [2, 3],
    "tag_ids": [1],
    "measuring_unit": "pz",
    "qty_for_unit": 1,
    "sku": "ZR-001",
    "metadata": {
      "titolo": "T-shirt",
      "autore": null,
      "descrizione": null,
      "robots": null,
      "open_graph_titolo": "T-shirt",
      "open_graph_url": null,
      "open_graph_immagine": null,
      "open_graph_tipo": null,
      "twitter_card": null,
      "twitter_site": null,
      "twitter_creator": null,
      "twitter_titolo": "T-shirt",
      "twitter_descrizione": null,
      "twitter_src": null
    },
    "inventory": {
      "stock": 25,
      "stock_min": 3,
      "minimum_reorder_quantity": 6,
      "reorder_lead_days": 14,
      "manage_stock": true,
      "price": 29.99
    }
  }
}

docs {
  summary:
  - Store products

  endpoint:
  - method: POST
  - path: /products

  query params:
  - include (string|array): Optional includes: variants, variants_options_table, brand, categories, collections, related_products, discounts, valid_discounts, expired_discounts, tags, product_type, tax_class, price_breakdown, price_lists (if enabled).

  path params:
  - none

  body:
  - JSON payload.
  - Available fields:
  - parent_id
  - brand_id
  - product_type_id
  - tax_class_id
  - name
  - slug
  - status
  - active
  - new
  - in_evidence
  - sku
  - ean
  - visible_from
  - visible_until
  - description
  - description_short
  - images (array|nullable)
  - images.*.id
  - images.*.file (file|image)
  - images.*.alt
  - images.*.name
  - images.*.mimetype
  - images.*.sort_order
  - images.*.type (nullable, thumb|cover)
  - measuring_unit (defaults to configured product.default_measuring_unit, `pz` by default)
  - qty_for_unit
  - length
  - width
  - height
  - weight
  - metadata (array)
  - category_ids (array)
  - category_ids.*
  - collection_ids (array)
  - collection_ids.*
  - related_product_ids (array)
  - related_product_ids.*
  - tag_ids (array)
  - tag_ids.*
  - inventory (object)
  - inventory.currency_id
  - inventory.stock
  - inventory.stock_reserved
  - inventory.stock_min
  - inventory.minimum_reorder_quantity
  - inventory.reorder_lead_days
  - inventory.manage_stock
  - inventory.price
  - inventory.price_includes_tax
  - inventory.purchase_price
  - Note: price_calculated.price_final includes tax when the selected price does not include tax, using the country header rate or configured default tax rate.
  - {locale}.name / name:{locale}
  - {locale}.slug / slug:{locale}
  - {locale}.description / description:{locale}
  - {locale}.description_short / description_short:{locale}
  - Note: for image upload use multipart/form-data with fields like `images[0][file]` and `images[0][type]=thumb`.
  - Note: uploaded product images are stored under date subfolders (`YYYY/MM/DD`) inside their image type folder.
  - Note: only one `thumb` and one `cover` are allowed. `type=null` can be repeated for generic images.
  - Note: files are not accepted in create; use update for file uploads.
}
