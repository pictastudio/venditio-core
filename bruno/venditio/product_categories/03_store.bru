meta {
  name: Store product_categories
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/product_categories
  body: json
  auth: inherit
}

params:query {
  ~include: tags,products_count,discounts,valid_discounts,expired_discounts
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

body:json {
  {
    "name": "Product Category 1",
    "abstract": "Short abstract",
    "description": "Long description",
    "metadata": {
      "titolo": "Product Category 1",
      "autore": null,
      "descrizione": "Long description",
      "robots": null,
      "open_graph_titolo": "Product Category 1",
      "open_graph_url": null,
      "open_graph_immagine": null,
      "open_graph_tipo": null,
      "twitter_card": null,
      "twitter_site": null,
      "twitter_creator": null,
      "twitter_titolo": "Product Category 1",
      "twitter_descrizione": null,
      "twitter_src": null
    },
    "show_in_menu": true,
    "highlighted": false,
    "sort_order": 1,
    "tag_ids": [1, 2],
    "visible_from": "2026-01-01 00:00:00",
    "visible_until": "2026-12-31 23:59:59"
  }
}

docs {
  summary:
  - Store product_categories

  endpoint:
  - method: POST
  - path: /product_categories

  query params:
  - include (string|array): Optional includes: tags, products_count, discounts, valid_discounts, expired_discounts.

  path params:
  - none

  body:
  - JSON payload.
  - Available fields:
  - parent_id
  - name
  - slug
  - abstract
  - description
  - metadata (array)
  - images (array|nullable)
  - images.*.id
  - images.*.file (file|image)
  - images.*.alt
  - images.*.name
  - images.*.mimetype
  - images.*.sort_order
  - images.*.type (nullable, thumb|cover)
  - active
  - show_in_menu
  - highlighted
  - sort_order (integer, starts at 1 within each tree branch)
  - tag_ids (array)
  - tag_ids.*
  - visible_from
  - visible_until
  - {locale}.name / name:{locale}
  - {locale}.slug / slug:{locale}
  - {locale}.abstract / abstract:{locale}
  - {locale}.description / description:{locale}
  - Slug fields are accepted only when venditio.slugs.editable_via_api (or the product_category override) is true; disabled input returns 422.
  - Omitted slugs are generated on create; on update, omitted slugs follow venditio.slugs.regenerate_on_update.
  - Note: for image upload use multipart/form-data with fields like `images[0][file]` and `images[0][type]=thumb`.
  - Note: uploaded catalog images are stored under date subfolders (`YYYY/MM/DD`) inside their image type folder.
  - Note: only one `thumb` and one `cover` are allowed. `type=null` can be repeated for generic images.
}
