meta {
  name: Update discounts
  type: http
  seq: 79
}

patch {
  url: {{baseUrl}}/discounts/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "discountable_type": "product_collection",
    "discountable_id": 1,
    "name": "Updated example",
    "standalone": false,
    "starts_at": null
  }
}

docs {
  summary:
  - Update discounts

  endpoint:
  - method: PATCH
  - path: /discounts/:id

  query params:
  - none

  path params:
  - id: Replace with target resource identifier.

  body:
  - JSON payload.
  - Available fields:
  - discountable_type (allowed: product, product_category, product_collection, product_type, brand, user)
  - discountable_id
  - type (allowed: percentage, fixed, fixed_price; fixed_price requires discountable_type=product)
  - value
  - name
  - code
  - active
  - starts_at (date|null; null is normalized to the current date)
  - ends_at
  - uses
  - max_uses
  - apply_to_cart_total
  - apply_once_per_cart
  - max_uses_per_user
  - one_per_user
  - free_shipping
  - minimum_order_total
  - priority
  - standalone
  - Note: product-scoped discounts are saved with maximum priority.
}
