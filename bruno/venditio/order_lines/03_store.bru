meta {
  name: Store order_lines
  type: http
  seq: 98
}

post {
  url: {{baseUrl}}/order_lines
  body: json
  auth: inherit
}

params:query {
  ~include: discounts,valid_discounts,expired_discounts
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "name": "Example"
  }
}

docs {
  summary:
  - Store order_lines

  endpoint:
  - method: POST
  - path: /order_lines

  query params:
  - include (string|array): Optional includes: discounts, valid_discounts, expired_discounts.

  path params:
  - none

  body:
  - JSON payload.
  - Available fields:
  - order_id
  - product_id
  - discount_id
  - product_name
  - product_sku
  - discount_code
  - discount_amount
  - unit_price
  - unit_discount
  - unit_final_price
  - unit_final_price_tax
  - unit_final_price_taxable
  - qty
  - total_final_price
  - tax_rate
  - product_data (array)
  - product_data is normalized to compact product, inventory, and pricing snapshot fields; extra keys are ignored.
}
