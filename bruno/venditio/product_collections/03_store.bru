meta {
  name: Store product_collections
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/product_collections
  body: json
  auth: inherit
}

params:query {
  ~include: products,products_count,tags,discounts,valid_discounts,expired_discounts
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

body:json {
  {
    "name": "Summer Picks",
    "description": "Editorial collection for summer products",
    "metadata": {
      "titolo": "Summer Picks",
      "autore": null,
      "descrizione": "Editorial collection for summer products",
      "robots": null,
      "open_graph_titolo": "Summer Picks",
      "open_graph_url": null,
      "open_graph_immagine": null,
      "open_graph_tipo": null,
      "twitter_card": null,
      "twitter_site": null,
      "twitter_creator": null,
      "twitter_titolo": "Summer Picks",
      "twitter_descrizione": null,
      "twitter_src": null
    },
    "tag_ids": [1, 2],
    "active": true,
    "visible_from": "2026-06-01 00:00:00",
    "visible_until": "2026-08-31 23:59:59"
  }
}

docs {
  summary:
  - Store product_collections

  endpoint:
  - method: POST
  - path: /product_collections

  query params:
  - include (string|array): Optional includes: products, products_count, tags, discounts, valid_discounts, expired_discounts.

  path params:
  - none

  body:
  - JSON payload.
  - Available fields:
  - name
  - slug
  - description
  - metadata (array)
  - tag_ids (array)
  - tag_ids.*
  - images (array|nullable)
  - images.*.id
  - images.*.file (file|image)
  - images.*.alt
  - images.*.name
  - images.*.mimetype
  - images.*.sort_order
  - images.*.type (nullable, thumb|cover)
  - active
  - visible_from
  - visible_until
  - {locale}.name / name:{locale}
  - {locale}.slug / slug:{locale}
  - {locale}.description / description:{locale}
  - Slug fields are accepted only when venditio.slugs.editable_via_api (or the product_collection override) is true; disabled input returns 422.
  - Omitted slugs are generated on create; on update, omitted slugs follow venditio.slugs.regenerate_on_update.
  - Note: for image upload use multipart/form-data with fields like `images[0][file]` and `images[0][type]=thumb`.
  - Note: uploaded catalog images are stored under date subfolders (`YYYY/MM/DD`) inside their image type folder.
  - Note: only one `thumb` and one `cover` are allowed. `type=null` can be repeated for generic images.
}
