meta {
  name: Store product_types
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/product_types
  body: json
  auth: inherit
}

params:query {
  ~include: discounts,valid_discounts,expired_discounts
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

body:json {
  {
    "name": "T shirts"
  }
}

docs {
  summary:
  - Store product_types

  endpoint:
  - method: POST
  - path: /product_types

  query params:
  - include (string|array): Optional includes: discounts, valid_discounts, expired_discounts.

  path params:
  - none

  body:
  - JSON payload.
  - Available fields:
  - name
  - slug
  - active
  - is_default
  - {locale}.name (locale object notation)
  - {locale}.slug (locale object notation)
  - name:{locale} (locale suffix notation)
  - slug:{locale} (locale suffix notation)
  - Slug fields are accepted only when venditio.slugs.editable_via_api (or the product_type override) is true; disabled input returns 422.
  - Omitted slugs are generated on create; on update, omitted slugs follow venditio.slugs.regenerate_on_update.
}
