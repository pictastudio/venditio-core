meta {
  name: Show orders
  type: http
  seq: 32
}

get {
  url: {{baseUrl}}/orders/:id
  body: none
  auth: inherit
}

params:query {
  ~include: lines,shipping_method,shipping_status,shipping_zone,user,discounts,valid_discounts,expired_discounts
}

params:path {
  id: 
}

headers {
  Accept: application/json
  Content-Type: application/json
}

docs {
  summary:
  - Show orders

  endpoint:
  - method: GET
  - path: /orders/:id

  query params:
  - include (string|array): Optional includes: lines, shipping_method, shipping_status, shipping_zone, user, discounts, valid_discounts, expired_discounts.
  - Note: show responses still include lines, shipping_method, and shipping_zone by default.

  path params:
  - id: Replace with target resource identifier.

  body:
  - none
}
